RacingJS.registerClass('Car < DisplayObject', function (options) {

    console.log('car.init', options);

    self.x = 0;
    self.y = 0;
    self.direction = 0;

    // Acceleration
    self.accelerate = function () {

    };

    // Render method
    self.render = function () {



    };

});
